{
  "scripts": {
    "test-daemon": "if [ -d ./daemon-build ]; then mocha test/*test.js; else echo \"Please run yarn link-daemon first.\"; exit 1; fi",
    "debug-daemon": "mocha test/*test.js debug",
    "setup-bluzelle-js": "git submodule update --init --recursive; cd bluzelle-js && yarn && webpack",
    "link-daemon": "./scripts/daemon-linker.sh"
  },
  "dependencies": {
    "async-wait-until": "^1.2.4",
    "bluebird": "^3.5.3",
    "chai": "^4.1.2",
    "dotenv": "^6.1.0",
    "fs": "^0.0.1-security",
    "http-proxy": "^1.17.0",
    "lodash": "^4.17.10",
    "mocha": "^5.2.0",
    "split": "^1.0.1",
    "uuid": "^3.3.2",
    "ws": "^5.2.0"
  }
}
