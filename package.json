{
  "scripts": {
    "test-daemon": "port=50000 address=localhost quiet=true mocha test/*.js --exit --timeout 10000",
    "setup-daemon": "cp -R ./configs/. ./daemon-build/output/"
  },
  "dependencies": {
    "async-wait-until": "^1.2.4",
    "chai": "^4.1.2",
    "fs": "^0.0.1-security",
    "lodash": "^4.17.10",
    "mocha": "^5.2.0",
    "ws": "^5.2.0"
  }
}
